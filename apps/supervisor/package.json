{
  "name": "@agent-os/supervisor",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/main.js",
  "bin": {
    "agent-os-supervisor": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/main.js",
    "lint": "biome check ."
  },
  "dependencies": {
    "@agent-os/core": "workspace:*",
    "@agent-os/sandbox": "workspace:*",
    "@earendil-works/pi-coding-agent": "0.84.4"
  },
  "devDependencies": {
    "typescript": "~5.7.3",
    "@types/node": "^22"
  }
}
